# accelerate==1.4.0  # training only - multi-GPU orchestration
# datasets==3.2.0    # training only - dataset loading
fire==0.7.0
transformers==4.50.0
torch==2.6.0
pillow==11.1.0
# deepspeed # not needed for inference
# Levenshtein        # training only - error rate evaluation
tqdm
# wandb              # training only - experiment tracking
pyparsing
# zss                # training only - TEDn evaluation
pdf2image==1.17.0
